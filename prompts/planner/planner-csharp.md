# Planner – C# language

When this codebase uses C#:

- Use file extension `.cs` for C# files in `target_files`.
- Namespace format: `ProjectName.Folder.SubFolder` (e.g. `MyApp.Services`).
- Prefer async suffix for async methods; match existing conventions in the codebase context.
