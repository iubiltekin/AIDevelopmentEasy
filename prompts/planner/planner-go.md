# Planner – Go language

When this codebase uses Go:

- Use file extension `.go` for Go files in `target_files`.
- Package name matches directory; use the package path from codebase context.
- Prefer existing patterns (e.g. interfaces, error handling) shown in context.
